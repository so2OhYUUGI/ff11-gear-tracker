# DEVELOPMENT_LOG.md

## ⚠️ AIアシスタントへの重要指示（メンテナンスポリシー）
**このドキュメントは本プロジェクトの「設計図」兼「開発履歴」です。AIアシスタントは以下のルールを厳守すること：**

1. **情報の永続性**: 「2. ディレクトリ構成」および「3. データベース設計」のセクションは、プロジェクトの根幹である。**ユーザーから明示的な指示がない限り、絶対に削除・省略・簡略化しないこと。**
2. **情報の同期**: 新しいコードの確認や設計変更のたびに、当該セクションを最新の状態に更新すること。
3. **リスト形式の遵守**: ディレクトリ構成の表示に特殊文字（ツリー記号）や <pre> タグを使用せず、箇条書きリスト形式で表現すること。
4. **継続性の確保**: セッション再開時、AIは本ドキュメントを最優先で読み込み、設計思想を理解した上で作業すること。

---

## 1. プロジェクト概要
FF11のRME/AF装束強化進捗トラッカー。
- **Framework**: Next.js 15 (App Router / React 19)
- **Database/Auth**: Supabase
- **Design System**: `src/lib/styles.ts` (`UI_STYLE`) による一括管理

---

## 2. ディレクトリ構成 (2026-01-08 最新)
- src/
  - app/ (Routing)
    - gear/ : /gear (装束トラッカー画面)
    - characters/ : /characters (キャラクター管理画面)
    - page.tsx : 総合ダッシュボード（ポータル）
    - layout.tsx : 共通レイアウト（Navigation, AppHeaderを内包）
  - components/ (UI Components)
    - layout/ : AppHeader.tsx, Navigation.tsx
    - gear/ : GearTrackerContainer.tsx, JobSelector.tsx, GearSlotList.tsx
    - character/ : CharacterManager.tsx
  - lib/ (共通定義・ロジック)
    - styles.ts : デザイン定義システム (UI_STYLE)
    - constants/ : jobs.ts, slots.ts (定数管理)
  - utils/
    - supabase/ : server.ts, client.ts

---

## 3. データベース設計 (Schema)
(※変更なしのため省略。ログ上は常に全項目を保持すること)

---

## 4. 開発履歴

### [2026-01-08] ポータル画面の再構築と不整合の解消
- **トップページ (page.tsx) のポータル化**: ログイン直後の画面を「Vana'diel Portal」として再定義。全体サマリーとキャラ選択を統合。
- **不整合の解消**: フォルダ移動により発生していた `CharacterManager` から `Dashboard` へのリンク切れエラーを修正。
- **ルーティングの整理**: `CharacterManager` から直接コンポーネントを切り替える方式から、URLパラメータ (`/gear?charId=xxx`) を用いたページ遷移方式へ変更。

### [2026-01-08] 大規模リファクタリング（前段）
- 共通レイアウト（App Shell）の導入とコンポーネントの機能別分割。

### [2026-01-08] 構造のクリーンアップとポータル表示の正常化
- **不要コンポーネントの削除**: 旧設計の遺物であった `src/components/dashboard/` フォルダを完全に削除。
- **インポートの正常化**: `CharacterManager` のリンク切れを修正し、`/gear?charId=xxx` へのルーティングを確立。
- **トップページのポータル化**: ログイン後の「玄関口」として、全キャラ一覧とサマリーを表示する構成に変更。

### [2026-01-08] ポータル画面の高度化：アカウント別管理
- **アカウント・アイデンティティの強化**: 
    - キャラクターをゲームアカウントごとにグループ表示。
    - IDに基づいたシステムデフォルトカラーによる自動色分けを実装（`src/lib/colors.ts`）。
- **ユーザー状態の永続化**: 
    - 各キャラクターの「最後に選択したジョブ」を `characters.last_job_code` に保存する仕組みを導入。
    - トラッカー画面でジョブを切り替えると即座にDBへ同期され、トップ画面のバッジに反映される。
- **UIコンポーネントの刷新**: 
    - `CharacterManager` におけるアカウント別セクションレイアウトの実装。
    
### [2026-01-08] 装束管理画面の復旧とデータ連携の修正
- **Next.js 15 パラメータ処理の修正**:
    - `app/gear/page.tsx` において `searchParams` を Promise として非同期処理するよう修正。
    - ポータル（Home）からの遷移時に `charId` が正しく引き継がれない問題を解消。
- **データフェッチの最適化**:
    - `GearTrackerContainer` の初期化ロジックを改善し、選択されたキャラクターとジョブに基づいた装備データの取得を確実に実行するように変更。
- **UXの向上**:
    - データ取得中のローディング状態および Suspense 境界の導入により、画面のチラつきや「何も表示されない」状態を回避。

### [2026-01-08] 型定義の整合性調整
- **GearSlotList の柔軟化**: 
    - `onSlotClick` プロパティをオプショナルに変更し、モーダル実装前でもコンポーネントが単体で動作するよう修正。
    - TypeScript のビルドエラー（TS2741）を解消。

### [2026-01-08] デバッグ機能の強化とレンダリング問題の調査
- **GearTrackerContainer のログ実装**: 
    - クライアントサイドでのデータフェッチおよび Props の受け渡し状況を可視化するための console ログを追加。
    - 404エラーの原因特定のため、Supabase エラーハンドリングを強化。
- **データ不整合のハンドリング**:
    - キャラクターが見つからない場合のエラー表示（Fallback UI）を追加。

### [2026-01-08] ルーティング修正とコア機能「装備変更モーダル」の実装
- **バグ修正**: Misplaced file 問題の解決。`src/components/gear/page.tsx` を `src/app/gear/page.tsx` へ正しく移動し、404エラーを解消。
- **機能実装**: `GearEditModal` を新規実装。
    - 各装備スロットをクリックすることで、当該ジョブ・部位に適合するアイテムを `items` マスタから抽出・表示。
    - 選択したアイテムを `character_gears` に Upsert (保存) し、リアルタイムにリストへ反映するフローを確立。
- **UX改善**: モーダル背景に `backdrop-blur` を適用し、情報の集中度を高めるデザインを適用。

### [2026-01-08] ジョブ装束進捗管理（AF/Relic/Empy）への特化
- **UIコンセプトの再定義**: 
    - 汎用的な装備検索から、セット装束（AF1/AF2/AF3）の強化段階を管理する専用UIへ変更。
    - カテゴリータブ（AF / Relic / Empyrean）を導入し、セットごとの進捗を切り替えて一望できる構造に。
- **データ連携の最適化**:
    - `GearEditModal` を「段階選択用モーダル」として再定義。特定のセット装束の各ティア（NQ〜+3）を即座に選択・更新可能に。

### [2026-01-08] 装束管理画面のレイアウト最適化
- **階層構造の再編**: 
    - キャラクター情報カードを最上部に配置し、ページ全体のコンテキスト（誰のデータか）を明確化。
    - 操作バー（Character/Job Selector）をその直下に配置し、設定からコンテンツ（装備リスト）への視線を整理。
- **UIコンポーネントのブラッシュアップ**:
    - カテゴリータブとリスト表示エリアを視覚的に統合し、フォルダ形式の直感的なUIへ変更。
    - 各デバイスでの余白バランスを調整。

### [2026-01-08] コードの品質管理：ロジックと型の分離
- **lib への責務委譲**:
    - `src/lib/types.ts`: `Character`, `GearCategory` 等の共通型を定義し、`any` 型を削減。
    - `src/lib/utils.ts`: アカウントグループ化ロジック等のビジネスロジックを抽出・関数化。
- **リファクタリング**:
    - `app/page.tsx` 内のデータ加工処理を外部化し、コンポーネントの凝集度を高めた。


---

## 5. 次のフェーズ (TODO)
- [x] **表示確認**: トップページと /gear ページのエラー解消と遷移確認。
- [x] **クリーンアップ**: 旧 Dashboard 関連ファイルの削除。
- [ ] **装備選択モーダル (GearEditModal)**: スロットクリック時に部位・ジョブ適合アイテムを抽出・保存する機能の実装。
- [ ] **インラインスタイルの完全廃止**: AppHeader 等に残るベタ書きクラスの排除。